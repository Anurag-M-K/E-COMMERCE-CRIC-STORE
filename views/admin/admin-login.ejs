<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../css/admin-login.css">
</head>

<body>

   
    <div class="wrapper">
        <div class="logo">
            <img src="https://cdn140.picsart.com/296818972051211.png" alt="">
        </div>
        <div class="text-center mt-4 name">
           Cric Store
        </div>
        <form class="p-3 mt-3" action="/admin/home-admin" method="post" onsubmit="return validation()">
            <div class="form-field d-flex align-items-center">
                <span class="far fa-user"></span>
                <input type="text" name="username" id="user" placeholder="Username">
                <span id="usernameError"></span>
            </div>
            <div class="form-field d-flex align-items-center">
                <span class="fas fa-key"></span>
                <input type="password"  name="password" id="password"  placeholder="Password">
                <span id="passwordError"></span>
            </div>
            <button type="submit" class="btn mt-3">Login</button>
        </form>
        
    </div>

</body>
</html>

<script>
        function validation(){
        let flag=0
        let username1= document.getElementById('user').value.trim()
        
        let password1 = document.getElementById('password').value.trim()

        
        if(username1==''){
          document.getElementById('usernameError').innerHTML="Username Required"
          flag=1
        }else if(username.length<4){
          document.getElementById('usernameError').innerHTML="Enter atleast 3 character"
          flag=1
        }else{
          document.getElementById('usernameError').innerHTML=""
        }
    
        
    
        if(password1==''){
            document.getElementById('passwordError').innerHTML="Password Required"
            flag=1

        }else if(password1.length<4){
            document.getElementById("passwordError").innerHTML = "enter atleast 4 chaerecters"
        }
        
        else
        {
            document.getElementById('passwordError').innerHTML=""
        }
    
        if(flag==1){
            return false
        }
    
        }
    
    
        
</script>